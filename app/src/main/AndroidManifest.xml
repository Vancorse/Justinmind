<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fikrirahmanto.justinmind">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FrontActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterPage" />
        <activity android:name=".RegisterNotice" />
        <activity android:name=".MainRegistration" />
        <activity android:name=".RegisterName" />
        <activity android:name=".RegisOne" />
        <activity
            android:name=".RegisTwo"
            android:label="@string/title_activity_regis_two"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisThree" />
        <activity android:name=".RegisFive" />
        <activity android:name=".RegisSix"></activity>
    </application>

</manifest>